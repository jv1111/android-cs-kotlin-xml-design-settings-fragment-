<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DynamicSettingsFragment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DynamicSettingsFragment" parent="Base.Theme.DynamicSettingsFragment" />

    <style name="inputText" parent="ThemeOverlay.Material3.MaterialTimePicker.Display.TextInputEditText">
    </style>

<!-- parent is the a set of style that you can override using <item>your style/color<item/> -->
<!--night mode theme-->
    <style name="nightModeSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/night_mode</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
        <item name="android:colorForeground">#989898</item>
    </style>
<!--notification theme-->
    <style name="notificationSwitch"  parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/notifications</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
        <item name="android:colorForeground">#989898</item>
    </style>
</resources>